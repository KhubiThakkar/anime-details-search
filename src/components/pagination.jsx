import React, { useState } from 'react';
import {Link} from "react-router-dom"

function Pagination({details, isEnabled}) {
  if(!isEnabled){
    return null;
  }
  return(
    <div>
      
    </div>
  )
}

export default Pagination;